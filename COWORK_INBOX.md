# 📥 COWORK_INBOX — Primera orden al abrir Renova (Operador / VERTEX)

> Cuando Franco inicie una nueva sesión de Cowork **desde la carpeta `renova`**, esto es lo primero que
> debe leer. Franco solo dirá: **"vertex, lee COWORK_INBOX.md"**.

## Dónde quedamos (12-sep-2026, cierre)
- Renova pivotó a **servicios del hogar**. La landing es **réplica fiel de `prerenova`** con las **fotos
  reales aprobadas** conectadas. Todo en la rama `feature/pivot-home-services` con **PR #1** abierto.
- Se montó el **equipo de agentes**: Gerente (`CLAUDE.md`) + 5 departamentos (`.claude/agents/`) +
  organigrama (`docs/departamentos/`).
- El **merge quedó listo a un clic**, esperando SOLO que Franco lo apruebe tras ver `npm run dev`.

## Primera tarea de la próxima sesión
1. Preguntar a Franco: **¿ya revisaste la landing con `npm run dev` y apruebas el merge?**
   - Si SÍ → guiarlo en el merge del PR #1 (o coordinar con Claude Code) y marcar Renova como oficial.
   - Si NO → ayudarlo a levantarla y revisarla primero.
2. Revisar `tasks/PENDING.md` y retomar lo pendiente.

## Pendientes vivos (product-level)
- Fotos reales de **Maintenance** y **Cleaning**.
- **Número de WhatsApp** real para el estimador.
- Pasar el repo **`Renova-` a PRIVADO** en GitHub (hoy público).

## Reglas de siempre
- Nada a producción ni merge sin aprobación de Franco. Secretos fuera del repo (VI.12).
- Explicaciones ligeras/visuales. Limpieza reversible (`_PARA_BORRAR`), nunca borrar directo.
- El "cerebro" (estrategia, doctrina, bandeja global) sigue en `vertex-pather`; aquí vive el producto.

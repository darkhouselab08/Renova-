# Tareas pendientes — Renova

> Lista viva de trabajos asignados. Al completar, mover a `DONE.md` con la fecha.

## En curso / pendientes

**El PR #1 está listo y espera SOLO la aprobación de merge de Franco** (ver `docs/OWNERSHIP.md`).
Merge sugerido cuando Franco dé luz verde tras revisar `npm run dev`:

```bash
gh pr merge 1 --merge   # o --squash, a preferencia de Franco
```

### Faltantes de datos reales (bloquean el 100%)
- Fotos reales de **Maintenance** y **Cleaning** (por ahora placeholder en gradiente elegante).
- **Número de WhatsApp** real para el estimador (hoy `WA_NUMBER` en `RenovaLanding.tsx` es un
  placeholder).
- Conexión real a Supabase (leer/escribir leads del estimador).

## Hecho recientemente
- PIVOTE-01: pivote del scaffold a servicios del hogar (identidad Dark Luxury, plomería Supabase).
- PIVOTE-02: landing reconstruida como réplica fiel de `prerenova`.
- PIVOTE-03: fotos reales conectadas (Power-Washing, Painting, hero, folio), restos de muebles
  apartados en `_PARA_BORRAR_media/`, PR #1 listo para oficializar.
